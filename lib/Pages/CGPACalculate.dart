import 'package:flutter/material.dart';

class CGPACalculate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CGPA Calculation'),
      ),
      body: Center(
        child: Text('CGPA Calculation Page'),
      ),
    );
  }
}
